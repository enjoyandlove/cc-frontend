export enum AssessType {
  Engagements = 0,
  Services = 1,
  Events = 2,
  Orientation = 3
}

export enum FilterType {
  attendees = 0,
  feedback = 1,
  rating = 2
}

export enum PersonaType {
  app = 0,
  web = 1
}

export enum PersonaPermission {
  forbidden = -1
}
