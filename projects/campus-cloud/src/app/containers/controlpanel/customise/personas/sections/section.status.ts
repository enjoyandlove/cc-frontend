export enum CategoryDeleteErrors {
  'NON_EMPTY_CATEGORY' = 'category non-empty'
}

export enum CampusGuideId {
  temporary = -1
}

export enum CampusGuideType {
  featured = 'featured',
  temporary = 'temporary'
}
