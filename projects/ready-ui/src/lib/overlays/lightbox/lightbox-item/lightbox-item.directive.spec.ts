import { LightboxItemDirective } from './lightbox-item.directive';

describe('LightboxItemDirective', () => {
  it('should create an instance', () => {
    const directive = new LightboxItemDirective();
    expect(directive).toBeTruthy();
  });
});
