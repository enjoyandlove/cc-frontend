import { ReadyInputDirective } from './ready-input.directive';

describe('ReadyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadyInputDirective();
    expect(directive).toBeTruthy();
  });
});
