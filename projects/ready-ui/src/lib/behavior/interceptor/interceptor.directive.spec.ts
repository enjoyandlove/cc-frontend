import { InterceptorDirective } from './interceptor.directive';

describe('InterceptorDirective', () => {
  it('should create an instance', () => {
    const directive = new InterceptorDirective();
    expect(directive).toBeTruthy();
  });
});
