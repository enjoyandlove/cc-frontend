export enum hasData {
  'yes' = 'Yes',
  'no' = 'No'
}

export enum ServiceAttendance {
  'enabled' = 1,
  'disabled' = 0
}

export enum ServiceFeedback {
  'enabled' = 1,
  'disabled' = 0
}

export enum Assessment {
  'on' = 'On',
  'off' = 'Off'
}

export enum Feedback {
  'enabled' = 'Enabled',
  'disabled' = 'Disabled'
}
