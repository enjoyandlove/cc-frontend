import { WidthToClassPipe } from './width-to-class.pipe';

describe('WidthToClassPipe', () => {
  it('create an instance', () => {
    const pipe = new WidthToClassPipe();
    expect(pipe).toBeTruthy();
  });
});
